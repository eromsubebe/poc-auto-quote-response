# API URL for the backend service
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (Cloud Run): do NOT set NEXT_PUBLIC_API_URL.
# Instead set BACKEND_URL at deploy time so Next.js rewrites /api/* to the backend.
# BACKEND_URL=https://your-backend-...run.app
